import React from 'react';

/**
 * This is basically just a placeholder component.  Wherever
 * we use this in the mdx files, the `inject-example-code-snippet`
 * plugin will transform this placeholder into an instance
 * of `SdkExampleFileTabsImpl` that includes all of the code snippets,
 * loaded from files in the SDK repos. For each language you may specify
 * a file path to load content from.
 *
 * @param snippetId: string
 * @constructor
 */
export const SdkExampleFileTabs = ({javascript, python, java, go, csharp, php, rust, ruby, elixir, swift, cli}) => {};



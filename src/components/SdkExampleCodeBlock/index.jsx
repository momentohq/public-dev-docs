import React from 'react';

/**
 * This is basically just a placeholder component.  Wherever
 * we use this in the mdx files, the `inject-example-code-snippet`
 * plugin will transform this placeholder into an instance
 * of `SdkExampleCodeBlockImpl` that includes the code snippet for
 * the specified language, loaded from the SDK repo.
 *
 * @param language: the SDK language
 * @param snippetId: ExampleSnippetId
 * @constructor
 */
export const SdkExampleCodeBlock = ({language, snippetId}) => {};


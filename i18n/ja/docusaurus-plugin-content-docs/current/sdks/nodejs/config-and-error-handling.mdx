---
sidebar_position: 2
sidebar_label: Config and Error Handling
title: Manage Momento Configuration and Error Handling in TypeScript and Node.js
description: Learn how to configure your Momento CacheClient and write production-ready error handling code in TypeScript and Node.js
keywords:
  - momento
  - cache
  - configuration
  - error handling
  - exceptions
  - sdk
  - production ready
  - typescript
  - node.js
  - nodejs
  - javascript
---

import { SdkExampleCodeBlock } from "@site/src/components/SdkExampleCodeBlock";
// This import is necessary even though it looks like it's un-used; The inject-example-code-snippet
// plugin will transform instances of SdkExampleCodeBlock to SdkExampleCodeBlockImpl
import { SdkExampleCodeBlockImpl } from "@site/src/components/SdkExampleCodeBlockImpl";

# Momento Node.js SDK で構成とエラー処理を管理する方法

以下のコードは、「CacheClient」を構築する最も簡単な方法を示しています。

<SdkExampleCodeBlock language={'javascript'} snippetId={'configuration_ConstructWithNoConfig'} />

ただし、「Configuration」オブジェクトを渡して動作をカスタマイズすることもできます。

Momento は、「InRegion」 (構成済み) など、「Configurations」 モジュールでいくつかの事前に構築された構成を提供します。
同じ AWS リージョン内からのサーバー側の接続に適したタイムアウトと接続数を使用します)、
および「Lambda」 (AWS Lambda 環境で使用するために調整されています)。 「Lambda」構成を指定する方法は次のとおりです。

<SdkExampleCodeBlock language={'javascript'} snippetId={'configuration_ConstructWithLambdaConfig'} />

:::tip
設定を省略した場合、Momento はデフォルトで「Laptop」設定を使用します。この構成は緩和されました
タイムアウト。開発環境または待ち時間の長い環境に適しています。 (本番環境やサーバー側での使用はお勧めしません。)
:::

「構成」オブジェクトの詳細については、[SDK 構成オブジェクト](/cache/develop/basics/client-configuration-objects.md) を参照してください。

デフォルトでは、`CacheClient` エラーは、スローされるのではなく、呼び出しの戻り値の一部として開発者に表示されます。
例外。これにより、コードを作成するときにエラーがより目立つようになり、IDE がエラーを確認するのにさらに役立つようになります。
気になるエラーは対処できました。

「get」呼び出しでエラーをチェックする方法の例を次に示します。

<SdkExampleCodeBlock language={'javascript'} snippetId={'configuration_ErrorHandlingHitMiss'} />

ただし、例外をスローしたい場合は、そうするように `CacheClient` を設定できます。

<SdkExampleCodeBlock language={'javascript'} snippetId={'configuration_ConstructWithThrowOnErrorsConfig'} />

この構成設定では、エラーが発生すると `SdkError` のインスタンスがスローされます。
それをキャッチし、その `.errorCode()` メソッドを使用して、発生した特定のエラーを特定します。

<SdkExampleCodeBlock language={'javascript'} snippetId={'configuration_ErrorHandlingExceptionErrorCode'} />

Momento でのエラー処理の詳細については、[SDK エラー処理](/cache/develop/basics/error-handling-production-readiness.md) を参照してください。
